Run the test suite, create a commit, and push to master.

1. Run `uv run pytest` to execute the test suite
2. If tests pass, stage all changes with `git add -A`
3. Create a commit with a descriptive message based on the changes
4. Push to master branch

If any step fails, stop and report the error to the user.
